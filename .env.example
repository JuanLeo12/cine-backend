# ===================================
# 🔐 VARIABLES DE ENTORNO - BACKEND CINE
# ===================================

# 📊 Base de datos PostgreSQL
DB_NAME=cine_bd
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432

# � Seguridad y Autenticación
BCRYPT_SALT_ROUNDS=10
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=1h

# 🚀 Servidor
PORT=4000

# 📝 NOTAS PARA DESPLIEGUE EN LA NUBE:
# 1. Cambia DB_HOST por la URL de tu base de datos en la nube
# 2. Genera un JWT_SECRET seguro: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. NUNCA subas el archivo .env real a Git
# 4. Configura las variables de entorno en tu servicio de hosting
